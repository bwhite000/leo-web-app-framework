export default class Resources {
    private resourcesTemplateElm;
    private static readonly NotFoundException;
    constructor();
    getString(id: string): string;
    private static assertElmExists(elm);
}
